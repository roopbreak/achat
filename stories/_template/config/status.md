# 상태창 형식

> 이 파일이 존재하면 카드별 정의를 우선 사용합니다.
> 임포트 시 AChat 카드의 character_book entries / scenario 코드블록에서 추출됩니다.
> 추출 결과가 없으면 이 파일을 생성하지 않고, story-narration 스킬의 기본형을 사용합니다.

---

## 상태창 형식 (카드 정의)

```
{카드에서 추출된 상태창 코드블록}
```

## 변동 규칙

{카드에서 추출된 상태창 변동·표시 규칙}

## 선택지 규칙

<!-- 카드에 ① ② ③ 선택지 규칙이 정의된 경우에만 이 섹션이 생성됩니다. -->
<!-- 정의되지 않은 카드는 이 섹션 없음 → 매 턴 선택지 출력 안 함. -->

{카드에서 추출된 선택지 규칙}
