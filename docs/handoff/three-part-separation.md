# HANDOFF: 서술부/info부/선택지부 3분할 개편

> 참조 플랜: docs/plan/three-part-separation_2026-06-11.md
> 상태: 활성 (설계 초안 — 미착수) | 마지막 업데이트: 2026-06-11

## 현재 상태

사용자가 "큰 개편"으로 규정하고 **새 세션 검토를 명시 지시**. 직전 세션에서 설계 논의만 하고
실제 설계·구현은 새 세션으로 이연. 이 문서는 그 인계용.

**3분할 토대는 이미 배포됨** (상태창 본문 분리 작업 b6c0cf2~a2133e7). 빠진 것은:
① 스토리별 on/off·형식 설정, ② info부 상단화(중간 상태창 난제 근본 해결), ③ 선택지부 형식 설정.

**+ 커맨드부 추가 요청 (2026-06-11)**: 명령어(`~행동~`/`~~`/`~!` 등 6종)가 NARRATION_RULES에
하드코딩됨. 이를 레지스트리 데이터로 분리 → 어드민 등록 UI + 채팅 클릭 버튼화. 같은 개편의
4번째 모듈로 편입(저장방식·어드민UI·클릭전송 공유). 상세 plan §2-4.

## TODO 체크리스트

- [ ] info 상단화 프롬프트 실험(코드 개편 전 모델 안정성 관측, 1~2 스토리)
- [ ] 3파트 설정 저장 방식 결정 (DB 컬럼 / 카드 메타 / 혼합)
- [ ] migration + 어드민/StoryDetail 설정 UI
- [ ] info부 on/off, 형식(카드 정의 우선)
- [ ] 선택지부 on/off + 규칙 승격
- [ ] 커맨드부: NARRATION_RULES 하드코딩 → 레지스트리 분리 + 등록 UI + 클릭 버튼(plan §2-4)
- [ ] Codex 설계리뷰 → 코드리뷰 → master 머지 → 원격 검증

## 다음 세션 시작 가이드

1. `docs/plan/three-part-separation_2026-06-11.md` §2(핵심 논점)·§4(미결정) 읽기
2. **info 상단화부터 프롬프트 실험** — 이게 P5 불필요화 + 중간 상태창 근본 해결의 키
3. 미결정(§4)은 사용자 입력 필요 — 추정 진행 금지
4. 배포: 서버 master 체크아웃([[deploy-master-branch]]), 원격 검증 autosave 주의

## 핵심 결정 근거 (보존)

- **info 상단화 = 게임 HUD 모델**: 상단 info는 "이전 턴의 결과". 사용자 수긍. 이어쓰기가
  본문 꼬리에서만 일어나 중간 상태 삽입이 구조적으로 불가 → P5 불필요화 가능.
- **선택지는 본문 끝 유지**: 읽기 순서 HUD→서술→선택. `splitChoices` 그대로 호환.
- **실험 우선**: 모델이 "상태→본문" 순서를 안정적으로 지키는지 미검증 → 코드 개편 전 프롬프트 실험.
