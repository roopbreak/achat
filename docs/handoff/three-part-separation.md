# HANDOFF: 서술부/info부/선택지부 3분할 개편

> 참조 플랜: docs/plan/three-part-separation_2026-06-11.md
> 상태: 활성 (설계 확정 — P1 구현 착수 대기) | 마지막 업데이트: 2026-06-11

## 현재 상태

사용자가 "큰 개편"으로 규정하고 **새 세션 검토를 명시 지시**. 직전 세션에서 설계 논의만 하고
실제 설계·구현은 새 세션으로 이연. 이 문서는 그 인계용.

**3분할 토대는 이미 배포됨** (상태창 본문 분리 작업 b6c0cf2~a2133e7). 빠진 것은:
① 스토리별 on/off·형식 설정, ② info부 상단화(중간 상태창 난제 근본 해결), ③ 선택지부 형식 설정.

**+ 커맨드부 추가 요청 (2026-06-11, 명확화됨)**: **`!디버그`/`!요약`/`!음란모드` 류 `!`-접두
시스템 명령어** — 서술 마커(`~행동~`)가 아니라 **앱이 인터셉트하는 기능 트리거**. 레지스트리
정의 → 어드민 등록 + 채팅 클릭 버튼 팔레트(즉시 실행). kind 3종: client_toggle(`!디버그`) /
server_action(`!요약`=summarizer) / mode_toggle(`!음란모드`=NSFW directive). 상세 plan §2-4.

**+ 분량 체제 확정 (2026-06-11, 사용자 — plan D5~D7)**: **1순위 = 잘림 없는 자연 완결.**
구조 진단 — ① 다이얼 하나가 프롬프트 목표와 max_tokens를 겸해 "목표를 채우면 잘리는"
자충수(1800자≈2700tk+상태창·선택지 > 상한 3072), ② auto-continue 면제선(floor×0.5)으로
실효 하한 600자. 해법 = **max_tokens 고정 16K**(사실상 무제한 — 분량은 프롬프트·EOS가
결정, 상한은 폭주 재난 캡) + 다이얼=목표 밴드만 + 게이트형 OUTPUT_TARGETS + 리서치 레버
R1~R4 + **auto-continue 백스톱 유지·강화**(floor 목표 하한 정렬·면제선 0.7). 수동 "이어서"
버튼·continue_mode는 **폐기**(`~~`·재생성과 중복, 16K로 주 용도 소멸). 상태창 **bottom
확정**(top 보류 — 잘림 소멸로 동기 소실, P0/P4 보류). `!디버그` 패널에 글자수 정보 표시.
기대치: 잘림=확실 / 하한=백스톱 보장 / 자연 충족=확률적 → 매 턴 분량 로그로 측정-튜닝 루프.
상세: plan §3-6b.

## TODO 체크리스트

- [x] 3파트 설정 저장 방식 결정 → **DB 컬럼** (사용자 확정 2026-06-11, plan §1 D1~D4)
- [x] 통합 설계 v1 작성 (plan §3 — migration 011 / 커맨드 resolve / NARRATION_RULES 3분해)
- [x] Codex 설계리뷰 → 반영 완료 (plan §7 — critical 2건 포함)
- [x] 분량 체제 확정 (plan D5~D7 + §3-6b — 웹 리서치 R1~R4 포함)
- [x] P0/P4(info 상단화·top 모드) **보류** (D6 — bottom 확정, 잘림 빈도 관측 시만 재개)
- [x] P1: migration 011 + system_commands 파서 + NARRATION_RULES 3분해 + 분량 체제(16K·목표 밴드·게이트·R1~R4·R2 주입·floor 정렬·면제선 0.7) + 매 턴 분량 로그 + 프론트 다이얼 교체 — **로컬 E2E 검증 완료 2026-06-11** (band 해석·outputDebug SSE·[output] 로그 정상. 주의: contracts 수정 시 `npm run contracts:build` 필요 — dist 소비)
- [ ] (P2 시점) 기존 스토리 일괄 적용 — plan §8 (대부분 전역 자동 적용, mode_flags 백필만 P2 범위)
- [x] P2: 커맨드부 (contracts·resolve·인터셉트·modes/actions API·`!음란모드` 이관(스캔 제거+012 백필)·클릭 팔레트) + 디버그 글자수 패널 — 2026-06-11
- [x] P3: 어드민 UI (StoryDetail 응답구성 토글 + StoryEdit "응답 구성" 탭 — 명령어 CRUD·directive) — 2026-06-11
- [x] 로컬 통합 테스트 — resolve/오버라이드/directive 주입/status off 조립/modes·actions API/403 게이트/어드민 PUT/epic 밴드 E2E(2,921자 자연 완결) 전부 통과
- [ ] Codex 코드리뷰 → 반영 → master 머지 → 배포 → 원격 검증
- [ ] (배포 후) 분량 로그 관측 → 게이트·면제선 튜닝 루프

## 다음 세션 시작 가이드

**설계 확정 완료(2026-06-11) — Codex 리뷰·분량 리서치·사용자 결정(D1~D7) 전부 반영.**
plan 정독 순서: §1(확정 결정 D1~D7) → §3(스키마/모듈, 특히 §3-6b 분량 체제) → §4(P1~P3)
→ §7(Codex 리뷰 처리 내역).

1. P1부터 구현 (P0/P4는 보류 — D6).
2. 핵심 구현 포인트: migration 011(현행 보존 기본값), NARRATION_RULES 3분해(preset DSL
   불변), 분량 체제(16K 고정·목표 밴드 다이얼·게이트·R1~R4·floor 정렬·면제선 0.7·매 턴 로그),
   `!음란모드`는 "배선 추가"가 아니라 **이관**(기존 히스토리 텍스트 스캔 제거+백필 — plan §3-3,
   Codex critical), 기존 `stories.commands`(가이드 커맨드)와 신규 `system_commands` 혼용 금지,
   status off에서도 선택지용 센티넬 유지(plan §3-4 F1).
3. 배포: 서버 master 체크아웃([[deploy-master-branch]]), 원격 검증 autosave 주의.
4. 배포 후: 분량 로그·디버그 패널로 측정 → 게이트 문구·면제선 튜닝 루프.

## 핵심 결정 근거 (보존)

- **info 상단화 = 게임 HUD 모델**: 상단 info는 "이전 턴의 결과". 사용자 수긍. 이어쓰기가
  본문 꼬리에서만 일어나 중간 상태 삽입이 구조적으로 불가 → P5 불필요화 가능.
- **선택지는 본문 끝 유지**: 읽기 순서 HUD→서술→선택. `splitChoices` 그대로 호환.
- **실험 우선**: 모델이 "상태→본문" 순서를 안정적으로 지키는지 미검증 → 코드 개편 전 프롬프트 실험.
