# Codex 재검수 결과 (2026-05-14, 메인 세션 직접 호출)

` 단일 트리거로 축소하고, 프로필 출력 본문에서는 `Instagram`/`인스타` 표기를 제거하거나 치환해 잔류 발화를 차단해라.

3. **FAIL: 엔진 기본 규칙과 스토리 규칙 충돌이 아직 남아 있다.** 기존 보고서는 선택지/기본 스테이터스 충돌만 잡았는데, 더 큰 충돌은 `성적 장면 대사 라벨 금지`다([lib/context-builder.mjs:107](/Users/shepard/Workspace/achat/lib/context-builder.mjs:107)). 그런데 스토리는 description·post_history에서 계속 `"고은서 | ..."` 식 발화자 라벨을 강제한다([recheck-live-story_2026-05-14.json](/Users/shepard/Workspace/achat/docs/stories/sect-sisters/recheck-live-story_2026-05-14.json:4)). 포맷 우선순위가 갈라져 출력이 흔들릴 수밖에 없다.  
→ 액션: 발화자 명시를 유지하려면 description/post_history에서 “본문 라벨 고정”을 제거하고 INFO 박스 쪽 식별만 남겨라. 아니면 엔진 공통 규칙을 건드릴 수 없다면 이 스토리만 라벨 의존 설계를 버려야 한다.

4. **상시 로어 4개로 줄였지만 미래 정보 누출은 여전하다.** [1490]과 [1491]에 P4 전용 어조, `주인님`, `사랑해`, `임신시켜줘`, `김미연 등장 분기`가 전부 상주한다. “현재 구간일 때만 적용” 문구를 붙였다고 정보 노출이 사라지는 게 아니다. 초기 턴부터 모델 캐시에 후반 보상 구조와 종착 어휘가 깔려 있어, 장기적으로 그 방향으로 끌리는 편향은 남는다.  
→ 액션: 상시에는 구간명과 허용/금지만 두고, P3/P4의 구체 대사 샘플·최종 분기 보상 문구는 별도 키워드/단계 로어로 분리해 늦게 주입해라.

5. **보류 판단도 안이하다.** `Phase` 유지 보류가 회귀 회피로 포장됐지만, 라이브 상태를 보면 description·scenario·post_history·[1490]이 모두 `Phase/P1~P4/굴복도`를 중복 표기한다. 같은 상태를 두 축으로 설명하는 구조 자체가 오염원이다. “AI는 턴/단계 계산을 못한다”는 엔진 제약 아래서, 중복 상태 머신을 남겨두는 보류는 기술부채가 아니라 현재 진행형 결함이다.  
→ 액션: 최소한 사용자 노출 규칙에서는 `Phase`를 버리고 `💞 구간` 하나로 통일해라. 내부 메모 용어로만 남기든지, 표시 체계에서 완전히 지워라.

6. **NSFW 안전장치도 문구는 늘었지만 설계는 여전히 삐걱거린다.** [1491]은 “명시적 거절 시 즉시 중단”을 걸어두면서도 [1490] 31~70 구간에 `옷 벗기 명령` `자위 명령` `언어 굴욕`을 허용한다. 거절 기반 중단과 협박 기반 강제 순응이 같은 상시 블록 안에서 맞부딪혀, 모델이 어느 쪽을 우선할지 흔들린다. first_mes도 여전히 노출·자위·절정 이미지를 전면에 깔아 “프롤로그 예외”라는 해설 없이는 규칙보다 자극이 먼저 각인된다.  
→ 액션: 31~70 구간 허용 항목에서 “명령”보다 “협박에 밀린 제한적 반응”으로 수위를 낮추고, first_mes는 최소한 절정 직전 컷으로 잘라 초반 규칙 위반 인상을 줄여라.
tokens used
102,265
1. **FAIL: 키워드 오염이 아직 심하다.** `keywordMatch`가 순수 `includes()`이고 `assistant` 응답까지 스캔하는데([lib/context-builder.mjs:230-264](/Users/shepard/Workspace/achat/lib/context-builder.mjs:230), [1375]의 `사실은` `평소엔` `본성`, [1371]의 `영상` `사진` `자위`, [1372]의 `팬티`는 너무 범용적이다. first_mes 자체가 `자위` `영상` `사진` `좆물싸개`를 다 뿌려서 초반 4턴은 관련 로어가 반강제로 달라붙고, 이후에도 평범한 문장 하나로 재점화된다.  
→ 액션: [1375]는 사실상 전면 폐기 수준으로 키를 재설계하고, [1371]/[1372]는 `영상` `사진` `자위` `팬티` 같은 일반어를 버리고 `예쁜보지녀` `보지TV` `음침한 냄새녀` `검정봉투 직거래` 같은 복합 키로 좁혀라.

2. **FAIL: `!인스타` 수정이 반쪽짜리다.** [1376]을 `scan_depth=1`로 줄였지만 키에 `인스타` `instagram` `인스타그램`이 남아 있다. 명령 실행 직후 assistant 출력 본문에 그 단어들이 그대로 박히므로, 다음 턴 일반 입력에도 재트리거된다. 엔진이 recent assistant 메시지를 함께 스캔한다는 점을 제대로 못 막았다.  
→ 액션: [1376] 키를 `!인스타` 단일 트리거로 축소하고, 프로필 출력 본문에서는 `Instagram`/`인스타` 표기를 제거하거나 치환해 잔류 발화를 차단해라.

3. **FAIL: 엔진 기본 규칙과 스토리 규칙 충돌이 아직 남아 있다.** 기존 보고서는 선택지/기본 스테이터스 충돌만 잡았는데, 더 큰 충돌은 `성적 장면 대사 라벨 금지`다([lib/context-builder.mjs:107](/Users/shepard/Workspace/achat/lib/context-builder.mjs:107)). 그런데 스토리는 description·post_history에서 계속 `"고은서 | ..."` 식 발화자 라벨을 강제한다([recheck-live-story_2026-05-14.json](/Users/shepard/Workspace/achat/docs/stories/sect-sisters/recheck-live-story_2026-05-14.json:4)). 포맷 우선순위가 갈라져 출력이 흔들릴 수밖에 없다.  
→ 액션: 발화자 명시를 유지하려면 description/post_history에서 “본문 라벨 고정”을 제거하고 INFO 박스 쪽 식별만 남겨라. 아니면 엔진 공통 규칙을 건드릴 수 없다면 이 스토리만 라벨 의존 설계를 버려야 한다.

4. **상시 로어 4개로 줄였지만 미래 정보 누출은 여전하다.** [1490]과 [1491]에 P4 전용 어조, `주인님`, `사랑해`, `임신시켜줘`, `김미연 등장 분기`가 전부 상주한다. “현재 구간일 때만 적용” 문구를 붙였다고 정보 노출이 사라지는 게 아니다. 초기 턴부터 모델 캐시에 후반 보상 구조와 종착 어휘가 깔려 있어, 장기적으로 그 방향으로 끌리는 편향은 남는다.  
→ 액션: 상시에는 구간명과 허용/금지만 두고, P3/P4의 구체 대사 샘플·최종 분기 보상 문구는 별도 키워드/단계 로어로 분리해 늦게 주입해라.

5. **보류 판단도 안이하다.** `Phase` 유지 보류가 회귀 회피로 포장됐지만, 라이브 상태를 보면 description·scenario·post_history·[1490]이 모두 `Phase/P1~P4/굴복도`를 중복 표기한다. 같은 상태를 두 축으로 설명하는 구조 자체가 오염원이다. “AI는 턴/단계 계산을 못한다”는 엔진 제약 아래서, 중복 상태 머신을 남겨두는 보류는 기술부채가 아니라 현재 진행형 결함이다.  
→ 액션: 최소한 사용자 노출 규칙에서는 `Phase`를 버리고 `💞 구간` 하나로 통일해라. 내부 메모 용어로만 남기든지, 표시 체계에서 완전히 지워라.

6. **NSFW 안전장치도 문구는 늘었지만 설계는 여전히 삐걱거린다.** [1491]은 “명시적 거절 시 즉시 중단”을 걸어두면서도 [1490] 31~70 구간에 `옷 벗기 명령` `자위 명령` `언어 굴욕`을 허용한다. 거절 기반 중단과 협박 기반 강제 순응이 같은 상시 블록 안에서 맞부딪혀, 모델이 어느 쪽을 우선할지 흔들린다. first_mes도 여전히 노출·자위·절정 이미지를 전면에 깔아 “프롤로그 예외”라는 해설 없이는 규칙보다 자극이 먼저 각인된다.  
→ 액션: 31~70 구간 허용 항목에서 “명령”보다 “협박에 밀린 제한적 반응”으로 수위를 낮추고, first_mes는 최소한 절정 직전 컷으로 잘라 초반 규칙 위반 인상을 줄여라.
