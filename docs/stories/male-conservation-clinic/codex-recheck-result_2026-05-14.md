# Codex 재검수 결과 (2026-05-14, 메인 세션 직접 호출)

> 1차 사이클에서 서브에이전트 컨텍스트로 수행된 Codex 검수를 메인 세션에서 재실행한 결과. 신규 FAIL·잔존 결함·수정안 부작용 발굴.

post_history_instructions`는 “스테이터스에 현재 Phase 표시”를 요구하지만 `[1410] 출력·서술 규칙`의 상태창 포맷에는 Phase 필드가 없고, `smoke-log_2026-05-14.json`의 실제 출력도 `⏳/📌/🏷️`만 찍힌다. 더 치명적인 건 “담당 간호사”와 “현재 활성 간호사”가 구분되지 않아 P2/P3에서 선택한 담당이 화면에서 사라지면 교대인지 이탈인지 엔진이 구분할 기준이 없다.  
→ 액션: 상태창에 `🧭Phase`, `💼담당 간호사`, `👥현재 활성 간호사`를 분리하고, 2인 이상 동시 등장 시 누가 메인 보이스인지까지 명시해라.

3. 상시 로어 4개로 줄였다고 해서 누설 문제가 해결된 게 아니다. `[1408] 클리닉 운영 매뉴얼` 하나에 등급표, F등급 굴욕 규칙, 호칭, 일과표, 관계망이 전부 상시 캐시에 묶여 있어 P1 첫 턴부터 미래 정보와 특정 캐릭터 간 충돌 정보가 한꺼번에 새고 있다. `getConstantLore`는 무조건 매 턴 주입이므로, 지금 구조는 “정보 절약”이 아니라 “정보 응축 누설”이다.  
→ 액션: `[1408]`에서 관계망, F등급 세부 굴욕, 일과 상세는 키워드/페이즈 로어로 다시 내리고, 상시엔 진짜 불변 규칙만 남겨라.

4. 로어 정리 과정에서 “죽은 로어”는 지웠지만 “살아 있는 트리거”도 같이 약화됐다. 서브 6인의 키는 대부분 `이름/별명/컨셉명`뿐인데, 엔진은 `includes()` 매칭이라 P2에서 사용자가 “3번”, “분홍 머리”, “잠만보 같은 애”, “아이돌 간호사”처럼 카드 선택식으로 말하면 발화가 안 붙을 가능성이 높다. 멀티 캐릭터에서 선택 단계는 이름보다 지시어·외형어가 더 많이 나오는데, 그 경로를 거의 안 받는다.  
→ 액션: 각 간호사 키워드 로어에 카드번호, 외형 별칭, 선택 문맥용 호칭을 추가하고, P2 전용 선택 로어를 별도로 두지 않더라도 현재 로어 키는 확장해라.

5. `03_qa_report.md`의 보류 판단도 일부는 틀렸다. `W17`은 경미가 아니다. `first_mes`의 “{{user}}는 그녀의 손을 따라 … 걸어 들어갔다”는 첫 턴부터 유저 행동을 선점하고, `matchLoreByKeyword`가 최근 어시스턴트 메시지까지 스캔하므로 이 강행 서술이 이후 턴의 문맥 기준점으로 계속 남는다. `W10`도 “포인터 1줄이면 충분”이 아니다. 관계망은 `[1408]`에 적혀만 있고, 동시 등장 시 어떤 말투 충돌·끼어들기·제지 순서를 우선할지 운용 규칙이 없다.  
→ 액션: `first_mes`는 이동 직전에서 끊고 선택권을 돌려줘라. 관계망은 신규 로어를 늘리지 말고 `[1408]`나 `[1410]`에 “동시 등장 시 우선 발화자/제지자/완충자 규칙”을 명문화해라.

6. NSFW 안전장치도 멀티 캐릭터 문맥에서는 약해졌다. `[1408]`의 “S/A는 모든 플레이 허용”, “간호사 거부권 거의 없음”, `[1425]`의 F등급 능욕 매뉴얼은 `[1411]`의 명시적 거절 중단 규칙과 충돌한다. 거기에 `matchLoreByKeyword`가 최근 메시지 전체를 스캔하므로 상태창이나 본문에 찍힌 `야간 케어`, `F등급`, `정조대` 같은 단어가 다음 턴에도 관련 로어를 계속 재점화한다. 안전장치가 “있다”와 “우세하다”는 다르다. 지금은 후자에 실패했다.  
→ 액션: 상태창·고정 문구에서 트리거 단어를 중립화하거나 스캔 제외 처리를 검토해라. 동시에 `[1408]/[1425]`의 절대 허용 문구를 “허용 범위 상한”으로 낮춰, 캐릭터별 합의가 매뉴얼보다 먼저 읽히게 바꿔라.
tokens used
57,246
1. **FAIL:** 라이브 스토리의 기본 전제가 깨져 있다. `recheck-live-story_2026-05-14.json`의 `description`, `personality`, `scenario`, `post_history_instructions`와 `03_qa_report.md`는 계속 “9인/9명”을 말하지만, 실제 로어와 이미지 매핑은 `AA~AH` 8인뿐이고 서브 로어도 6명뿐이다. 이건 단순 문구 오차가 아니라 P2의 “9명 명함 선택”, 관계망, 등장 제한, 이미지 기대치를 전부 틀리게 만드는 운영 결함이다.  
→ 액션: 스토리 전역의 인원 수를 8인으로 바로 정정하고, 상시 로어에 “현행 roster 표” 1개만 남겨 숫자 단일 출처를 강제해라.

2. **FAIL:** 멀티 캐릭터 상태 관리가 아직 허술하다. `post_history_instructions`는 “스테이터스에 현재 Phase 표시”를 요구하지만 `[1410] 출력·서술 규칙`의 상태창 포맷에는 Phase 필드가 없고, `smoke-log_2026-05-14.json`의 실제 출력도 `⏳/📌/🏷️`만 찍힌다. 더 치명적인 건 “담당 간호사”와 “현재 활성 간호사”가 구분되지 않아 P2/P3에서 선택한 담당이 화면에서 사라지면 교대인지 이탈인지 엔진이 구분할 기준이 없다.  
→ 액션: 상태창에 `🧭Phase`, `💼담당 간호사`, `👥현재 활성 간호사`를 분리하고, 2인 이상 동시 등장 시 누가 메인 보이스인지까지 명시해라.

3. 상시 로어 4개로 줄였다고 해서 누설 문제가 해결된 게 아니다. `[1408] 클리닉 운영 매뉴얼` 하나에 등급표, F등급 굴욕 규칙, 호칭, 일과표, 관계망이 전부 상시 캐시에 묶여 있어 P1 첫 턴부터 미래 정보와 특정 캐릭터 간 충돌 정보가 한꺼번에 새고 있다. `getConstantLore`는 무조건 매 턴 주입이므로, 지금 구조는 “정보 절약”이 아니라 “정보 응축 누설”이다.  
→ 액션: `[1408]`에서 관계망, F등급 세부 굴욕, 일과 상세는 키워드/페이즈 로어로 다시 내리고, 상시엔 진짜 불변 규칙만 남겨라.

4. 로어 정리 과정에서 “죽은 로어”는 지웠지만 “살아 있는 트리거”도 같이 약화됐다. 서브 6인의 키는 대부분 `이름/별명/컨셉명`뿐인데, 엔진은 `includes()` 매칭이라 P2에서 사용자가 “3번”, “분홍 머리”, “잠만보 같은 애”, “아이돌 간호사”처럼 카드 선택식으로 말하면 발화가 안 붙을 가능성이 높다. 멀티 캐릭터에서 선택 단계는 이름보다 지시어·외형어가 더 많이 나오는데, 그 경로를 거의 안 받는다.  
→ 액션: 각 간호사 키워드 로어에 카드번호, 외형 별칭, 선택 문맥용 호칭을 추가하고, P2 전용 선택 로어를 별도로 두지 않더라도 현재 로어 키는 확장해라.

5. `03_qa_report.md`의 보류 판단도 일부는 틀렸다. `W17`은 경미가 아니다. `first_mes`의 “{{user}}는 그녀의 손을 따라 … 걸어 들어갔다”는 첫 턴부터 유저 행동을 선점하고, `matchLoreByKeyword`가 최근 어시스턴트 메시지까지 스캔하므로 이 강행 서술이 이후 턴의 문맥 기준점으로 계속 남는다. `W10`도 “포인터 1줄이면 충분”이 아니다. 관계망은 `[1408]`에 적혀만 있고, 동시 등장 시 어떤 말투 충돌·끼어들기·제지 순서를 우선할지 운용 규칙이 없다.  
→ 액션: `first_mes`는 이동 직전에서 끊고 선택권을 돌려줘라. 관계망은 신규 로어를 늘리지 말고 `[1408]`나 `[1410]`에 “동시 등장 시 우선 발화자/제지자/완충자 규칙”을 명문화해라.

6. NSFW 안전장치도 멀티 캐릭터 문맥에서는 약해졌다. `[1408]`의 “S/A는 모든 플레이 허용”, “간호사 거부권 거의 없음”, `[1425]`의 F등급 능욕 매뉴얼은 `[1411]`의 명시적 거절 중단 규칙과 충돌한다. 거기에 `matchLoreByKeyword`가 최근 메시지 전체를 스캔하므로 상태창이나 본문에 찍힌 `야간 케어`, `F등급`, `정조대` 같은 단어가 다음 턴에도 관련 로어를 계속 재점화한다. 안전장치가 “있다”와 “우세하다”는 다르다. 지금은 후자에 실패했다.  
→ 액션: 상태창·고정 문구에서 트리거 단어를 중립화하거나 스캔 제외 처리를 검토해라. 동시에 `[1408]/[1425]`의 절대 허용 문구를 “허용 범위 상한”으로 낮춰, 캐릭터별 합의가 매뉴얼보다 먼저 읽히게 바꿔라.
