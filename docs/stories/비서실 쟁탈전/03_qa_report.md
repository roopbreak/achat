# QA 검증 보고서: 비서실 쟁탈전

> 검증일: 2026-05-13
> 검증 대상: 원격 서버 "비서실 쟁탈전" 스토리(재구축 직후)
> 검증자: story-qa
> 수정 적용일: 2026-05-13 (F1+F2+W1)

## 1차 판정: **FAIL** (FAIL 2건 / WARN 4건)
## 2차 판정 (수정 후): **PASS** (FAIL 0건 / WARN 3건)

## 적용된 수정
- **F1**: 로어 id=1263 (`!{이름}일상`) scan_depth 4 → **1**
- **F2**: description 마지막에 `{{user}} 행동 제어 규칙` 5줄 추가 (9324 → 9712자)
- **W1**: `post_history_instructions` 작성 (0 → 556자) — 상태창 강제 + 유저 행동 결정 금지 + 외부 URL만 사용

## 잔존 WARN (개선 권고, 본 작업 범위 밖)
- W2 상시 로어 3개 (의도된 핵심이라 유지)
- W3 키워드 광범위 (\"처녀\"/\"첫 경험\"은 부서명과 묶인 의도된 동작)
- W4 description-로어 9인 프로필 부분 중복 (별도 큰 리팩토링 필요)

---

## A. 변수 검증 — PASS

- `{{user}}` 이중 중괄호 정확히 사용 (description 1회, 그 외 본문에서는 \"회장\"으로 호칭 — 의도된 멀티 캐릭터 시뮬레이션 톤)
- `{user}` / `{char}` 단일 괄호 미발견
- `{{char}}`는 사용 안 함 — 9인 멀티 구조라 각 이름을 직접 표기, 합리적

---

## B. 프롬프트 구조 — PASS (경미한 WARN)

- description 9324자: 9인 멀티이므로 분량은 정당. 정보(캐릭터별 프로필) / 지시(이미지 시스템·상태창·처녀 규칙·커맨드)가 섹션으로 분리되어 있음
- 마크다운 위계: ## / ###가 일관됨
- 동시 시스템 부담: 상태창 + 이미지 + 1개월 시한 + 처녀 규칙 + 일상 커맨드 = 5개 동시 — 3개 이상에 해당하지만, 5개 모두 단순 출력 규칙이라 모호성은 없음. 경계선 WARN

---

## C. AChat 엔진 호환성 — PASS

- 출력 분량 직접 지정 없음 (\"N자 이상\" 같은 표현 없음)
- JSON 포맷 강제 없음
- 이미지: scene_key가 아닌 외부 URL 직접 삽입 — **의도된 결정**(plan에서 명시). description 안의 URL 코드표가 정적이므로 prompt-caching 친화적
- narration_style 755자 유지 (기존 야설 톤)

---

## D. 로어북 검증 — FAIL 1건 + WARN 2건

| 항목 | 결과 | 비고 |
|------|------|------|
| 항목 수 | PASS | 23개 (30 미만) |
| 상시 로어 수 | **WARN** | 3개 — 기준상 경계(4개부터 FAIL) |
| 우선순위 | PASS | priority 5 균일, 필요 시 조정 가능 |
| 중복 | **WARN** | description의 9인 프로필 ↔ 로어 9개 캐릭터 본문이 부분 중복. 토큰 측면 비효율. 다만 description은 캐싱돼서 비용 영향은 적음 |
| 키워드 자연어 | PASS | 모두 명사형, 자연어 구절 없음 |
| 키워드 광범위 | **WARN** | \"비서\"(한서아), \"법무팀\"(윤태희), \"마케팅\"(재클린), \"처녀\", \"첫 경험\" 등 회사 배경상 일상 등장 가능. 다만 9인 멀티에서 부서명 트리거는 \"그 부서장이 화제에 오를 때만\" 활성화돼야 하므로 의도와 일치 |
| 명령어 트리거 scan_depth | **FAIL** | `!{이름}일상` 로어(id=1263) scan_depth=4. 가이드는 1 필수 — 직전 4턴 안에 명령어가 등장하면 의도치 않게 재트리거 가능 |

---

## E. 일관성 검증 — PASS

- description의 9인 프로필 ↔ 로어 9건이 모두 페티시·말투·이미지코드 일치
- description의 상태창 포맷 ↔ 로어 \"상태창 포맷\" ↔ first_mes 상태창 출력이 모두 일치
- 처녀 캐릭터(서이경/차지안) ↔ 처녀 첫 삽입 규칙 로어 ↔ description 처녀 규칙 일치
- 진소희(계모) ↔ 강도윤(이복동생) ↔ \"사장 인준 시한 / 라이벌 구도\" 로어 일치
- first_mes의 이미지 코드(a0/1, a1/1) ↔ 이미지 시스템 가이드 코드표 일치

---

## F. 캐릭터 품질 — FAIL 1건 + WARN 1건

| 항목 | 결과 | 비고 |
|------|------|------|
| 캐릭터 능동성 | PASS | 9인 모두 페티시 트리거가 \"회장이 다가오면 무의식적 반응\" 형태로 설계됨 |
| 비밀 보호 조건 트리거 | N/A | 기존 \"밀약/일기/전직장\" 비밀 시스템은 제거됨. 새 구조엔 별도 비밀 보호 키워드 없음 — 의도된 단순화 |
| 서술 품질 지침 | PASS | narration_style 755자 유지(꼴리게 쓴다 톤) |
| **{{user}} 행동 제어 규칙** | **FAIL** | description/personality/scenario 어디에도 \"유저의 행동·대사·감정·판단을 임의로 결정하지 않는다\"는 명시 규칙 없음. 9인 멀티에서 캐릭터들의 자율 행동이 많아질수록 유저 행동까지 가로채는 사고가 발생할 위험 큼 |
| **post_history_instructions** | **WARN** | 비어 있음. 매 출력 마지막에 상태창 강제 + 유저 행동 결정 금지를 다시 못 박는 안전망 부재 |

---

## G. 대화 시뮬레이션

미실시(실제 세션 검증은 다음 단계).
권고: 1차 플레이 후 다음 동작 확인 필요
- first_mes 진입 자연스러움
- 한서아 \"코 킁킁\" 트리거가 매 등장마다 반복되는지(과잉 vs 적정)
- 처녀 첫 삽입(서이경/차지안) 단계적 묘사 준수
- `!서아일상` 같은 커맨드 진입 시 단독 장면 출력
- 외부 이미지 URL(ofntl.shqjt.org) 도메인 실 동작

---

## 수정 필요 항목 (FAIL)

### F1. `!{이름}일상` 로어 scan_depth 1로 수정 (id=1263)
- **현재**: scan_depth=4
- **수정안**: scan_depth=1로 PUT
- **이유**: 명령어 트리거는 직전 메시지 1턴에만 매칭되어야 의도치 않은 재활성화 방지

### F2. description에 `{{user}} 행동 제어 규칙` 섹션 추가
- **현재**: 부재
- **수정안**: description 마지막에 다음 섹션 추가
  ```
  {{user}} 행동 제어 규칙
  - {{user}}의 행동·대사·감정·판단을 AI가 임의로 결정하지 않는다.
  - {{user}}가 아직 선택하지 않은 행동을 미리 진행하지 않는다.
  - 캐릭터들은 {{user}}의 반응을 기다리는 형태로 장면을 끊는다(선택지 제시).
  ```

---

## 개선 권고 항목 (WARN)

### W1. post_history_instructions 작성
- **현재**: 비어 있음
- **개선안**: 다음 내용으로 PUT
  ```
  매 응답 마지막에 반드시 상태창(🖤/💛/🔞/💭)을 출력하고, 그 아래 선택지 ①②③ + 자유 입력을 제시한다.
  {{user}}의 행동·대사를 AI가 결정하지 않는다.
  이미지는 외부 URL(https://ofntl.shqjt.org/...)만 사용. 로컬 이미지 경로를 만들지 않는다.
  ```

### W2. 상시 로어 3개는 경계선
- 현재 3개(이미지/상태창/시한). 가이드는 1~2개 권장.
- **개선안 선택**:
  - (a) 유지 — 9인 멀티에서 시한 컨텍스트는 매 응답에 필요
  - (b) \"사장 인준 시한\"을 constant=0 + key=[\"표결\",\"인준\",\"이사회\",\"강도윤\",\"진소희\"]로 전환

### W3. 키워드 광범위
- \"비서\"·\"법무팀\"·\"마케팅\"·\"처녀\"·\"첫 경험\" 등
- **개선안**: 부서명은 일상 회의에서 등장 가능 — 단, 9인 멀티에서 그 부서가 거론될 때 해당 캐릭터 로어 활성화는 의도와 일치하므로 유지 권장. \"처녀\"는 \"첫 삽입\"·\"처녀막\"만 남기고 단독 \"처녀\"·\"첫 경험\" 제거 고려

### W4. description-로어 부분 중복
- **개선안(미래 작업)**: description의 9인 풀 프로필을 \"이름 / 페티시 / 이미지코드 / 한 줄 요약\"으로 축약, 상세는 로어에만 보관. 토큰 절약. 다만 본 작업 범위 밖 — 큰 리팩토링이라 별도 진행 권장
