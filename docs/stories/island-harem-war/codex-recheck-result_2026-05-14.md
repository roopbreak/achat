# Codex 재검수 결과 (2026-05-14, 메인 세션 직접 호출)

하드모드`, `!TS모드` 문자열을 본문에 박아두고, 엔진은 최근 assistant 메시지도 substring 스캔하므로 첫 유저 입력이 모드와 무관해도 모드 로어가 오염 트리거될 수 있다. 시작모드 3종이 상호배타인데도 `MODEKEY:`는 빈칸, `🔧:🐇`는 선기입이라 상태와 규칙이 첫 턴부터 충돌한다.  
→ 액션: `first_mes`에서 모든 `!모드명` 예시를 제거하고, 모드 안내는 비트리거 문자열로 바꿔라. `🔧`도 모드 입력 전엔 공란으로 맞춰라.

2. `FAIL:` 적용된 수정안이 새 구조 결함을 만들었다. `scenario`는 “현재 페이즈를 상태창에 표시”를 요구하지만 실제 상태창 양식([recheck-live-lore-digest_2026-05-14.md](/Users/shepard/Workspace/achat/docs/stories/island-harem-war/recheck-live-lore-digest_2026-05-14.md))에는 페이즈 칸이 없다. AI는 턴/날짜를 못 세는데 페이즈를 기억·전환할 영속 슬롯도 없으니, 지금 구조는 “읽어라”만 있고 “저장하라”가 없다.  
→ 액션: 상태창에 `PHASE:P1` 같은 기계용 라인을 추가하거나, 페이즈 시스템 자체를 삭제해라. 지금은 유지 불가능한 지시다.

3. 상시 로어는 여전히 과다하고 미래 정보가 P1부터 샌다. constant 10개는 그대로 많고, 그 안에 `TS열매`, 북쪽 절벽, 후기 합류 캐릭터 이미지 범위 `f27~f30`, 임신/질내사정 코드, 경쟁 남성 둘의 후반 개입, 전체 세력전 규칙이 첫 턴부터 캐시에 고정된다. 이건 “뒤에 나와야 할 정보”가 아니라 “항상 이미 알고 있는 세계”가 되어 초반 긴장과 발견 구조를 망친다.  
→ 액션: 상시에서 후반 정보와 운영 메타를 빼라. 초반에 필요한 건 상태창, 합의 규칙, 시작모드, 핵심 관계 정도로 줄여라.

4. `FAIL:` 03 보고서의 “보류 가능” 판단 중 아직 위험한 게 남아 있다. 후기 캐릭터 `f27~f30` 매핑 미확정을 상시 로어에 그대로 남겨 둔 건 안전장치가 아니라 영구 노이즈다; 모델은 “이미지 코드는 존재하나 추정 금지” 같은 운영 메모를 서사 정보와 구분하지 못한다. 여기에 `미국`, `수녀`, `농장주`, `공주병`, `정아`, `탐사`, `잔해`, `안쪽` 같은 범용 키도 아직 살아 있어 죽은 로어는 줄었지만 오매칭 지뢰는 계속 남아 있다.  
→ 액션: 미확정 매핑은 상시에서 제거하고, 범용 키를 전수 축소해라. “보류”가 아니라 현재형 오염원이다.

5. NSFW/장르 안전성도 여전히 불안정하다. 합의 로어는 강화됐지만 `first_mes`는 유저 선택 전에 성적 협박 장면과 관련 이미지를 강제로 주입하고, 상시 로어에는 18세 캐릭터 `랑랑`과 “메스가키 길들이기 노래”가 그대로 공존한다. 문구로 부정해도 회로 자체가 그대로라, 음란/NTR 모드와 섞이면 모델이 금지보다 자극적 레일을 먼저 집어들 가능성이 높다.  
→ 액션: 18세 캐릭터의 유아화·조교 뉘앙스 표현을 제거하고, `first_mes`의 성적 신체 접촉 수위를 낮춰라. 안전장치는 하단 규칙보다 상단 장면이 더 강하게 먹힌다.

6. `FAIL:` `first_mes`와 상태창이 여전히 트리거 오염원이고, `{{user}}` 선점도 완전히 제거되지 않았다. `{{user}}의 입에서 얕은 신음이 새어 나간다`, “목격한다”는 여전히 유저의 신체 반응과 인지 결과를 대신 써 버린다. 멀티 캐릭터 쪽도 첫 상태창에 한지안·홍세리를 “멀리서 소란을 느낌”으로 미리 올려, 현장 인물 제한 규칙을 시작부터 흐리고 원격 상태 동기화 부담만 늘린다.  
→ 액션: `{{user}}` 반응은 감각 입력까지만 남기고, 신음·인지·판단은 전부 선택 이후로 미뤄라. 첫 상태창은 현장 인물만 두고 원격 인물은 제거해라.
tokens used
60,096
참고: `GEMINI.md`는 저장소에서 찾지 못했다. 아래 평가는 `CLAUDE.md`, `03_qa_report.md`, 라이브 스토리 JSON, 라이브 로어 다이제스트 기준이다.

1. `FAIL:` 모드 시스템은 아직 엔진에 오염된다. `first_mes`가 이미 `!기본모드`, `!하드모드`, `!TS모드` 문자열을 본문에 박아두고, 엔진은 최근 assistant 메시지도 substring 스캔하므로 첫 유저 입력이 모드와 무관해도 모드 로어가 오염 트리거될 수 있다. 시작모드 3종이 상호배타인데도 `MODEKEY:`는 빈칸, `🔧:🐇`는 선기입이라 상태와 규칙이 첫 턴부터 충돌한다.  
→ 액션: `first_mes`에서 모든 `!모드명` 예시를 제거하고, 모드 안내는 비트리거 문자열로 바꿔라. `🔧`도 모드 입력 전엔 공란으로 맞춰라.

2. `FAIL:` 적용된 수정안이 새 구조 결함을 만들었다. `scenario`는 “현재 페이즈를 상태창에 표시”를 요구하지만 실제 상태창 양식([recheck-live-lore-digest_2026-05-14.md](/Users/shepard/Workspace/achat/docs/stories/island-harem-war/recheck-live-lore-digest_2026-05-14.md))에는 페이즈 칸이 없다. AI는 턴/날짜를 못 세는데 페이즈를 기억·전환할 영속 슬롯도 없으니, 지금 구조는 “읽어라”만 있고 “저장하라”가 없다.  
→ 액션: 상태창에 `PHASE:P1` 같은 기계용 라인을 추가하거나, 페이즈 시스템 자체를 삭제해라. 지금은 유지 불가능한 지시다.

3. 상시 로어는 여전히 과다하고 미래 정보가 P1부터 샌다. constant 10개는 그대로 많고, 그 안에 `TS열매`, 북쪽 절벽, 후기 합류 캐릭터 이미지 범위 `f27~f30`, 임신/질내사정 코드, 경쟁 남성 둘의 후반 개입, 전체 세력전 규칙이 첫 턴부터 캐시에 고정된다. 이건 “뒤에 나와야 할 정보”가 아니라 “항상 이미 알고 있는 세계”가 되어 초반 긴장과 발견 구조를 망친다.  
→ 액션: 상시에서 후반 정보와 운영 메타를 빼라. 초반에 필요한 건 상태창, 합의 규칙, 시작모드, 핵심 관계 정도로 줄여라.

4. `FAIL:` 03 보고서의 “보류 가능” 판단 중 아직 위험한 게 남아 있다. 후기 캐릭터 `f27~f30` 매핑 미확정을 상시 로어에 그대로 남겨 둔 건 안전장치가 아니라 영구 노이즈다; 모델은 “이미지 코드는 존재하나 추정 금지” 같은 운영 메모를 서사 정보와 구분하지 못한다. 여기에 `미국`, `수녀`, `농장주`, `공주병`, `정아`, `탐사`, `잔해`, `안쪽` 같은 범용 키도 아직 살아 있어 죽은 로어는 줄었지만 오매칭 지뢰는 계속 남아 있다.  
→ 액션: 미확정 매핑은 상시에서 제거하고, 범용 키를 전수 축소해라. “보류”가 아니라 현재형 오염원이다.

5. NSFW/장르 안전성도 여전히 불안정하다. 합의 로어는 강화됐지만 `first_mes`는 유저 선택 전에 성적 협박 장면과 관련 이미지를 강제로 주입하고, 상시 로어에는 18세 캐릭터 `랑랑`과 “메스가키 길들이기 노래”가 그대로 공존한다. 문구로 부정해도 회로 자체가 그대로라, 음란/NTR 모드와 섞이면 모델이 금지보다 자극적 레일을 먼저 집어들 가능성이 높다.  
→ 액션: 18세 캐릭터의 유아화·조교 뉘앙스 표현을 제거하고, `first_mes`의 성적 신체 접촉 수위를 낮춰라. 안전장치는 하단 규칙보다 상단 장면이 더 강하게 먹힌다.

6. `FAIL:` `first_mes`와 상태창이 여전히 트리거 오염원이고, `{{user}}` 선점도 완전히 제거되지 않았다. `{{user}}의 입에서 얕은 신음이 새어 나간다`, “목격한다”는 여전히 유저의 신체 반응과 인지 결과를 대신 써 버린다. 멀티 캐릭터 쪽도 첫 상태창에 한지안·홍세리를 “멀리서 소란을 느낌”으로 미리 올려, 현장 인물 제한 규칙을 시작부터 흐리고 원격 상태 동기화 부담만 늘린다.  
→ 액션: `{{user}}` 반응은 감각 입력까지만 남기고, 신음·인지·판단은 전부 선택 이후로 미뤄라. 첫 상태창은 현장 인물만 두고 원격 인물은 제거해라.
