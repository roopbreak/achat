# Codex 재검수 결과 (2026-05-14, 메인 세션 직접 호출)

LIMIT 1'
lib/db.mjs:560:    'SELECT * FROM chat_sessions WHERE story_name = ? ORDER BY updated_at DESC'
lib/db.mjs:594:    ORDER BY exchange_number DESC
lib/db.mjs:601:    'SELECT * FROM messages WHERE session_id = ? ORDER BY exchange_number'
lib/db.mjs:607:    'SELECT * FROM messages WHERE session_id = ? AND summarized = 1 ORDER BY exchange_number'
lib/db.mjs:614:    ORDER BY exchange_number LIMIT ?
lib/db.mjs:655:    'SELECT * FROM save_slots WHERE story_name = ? ORDER BY created_at DESC'
lib/db.mjs:679:  return db.prepare('SELECT * FROM personas ORDER BY updated_at DESC').all();

codex
1. **FAIL:** `recheck-live-story_2026-05-14.json`의 `first_mes`가 아직도 `{{user}} | 동규야...` 대사, 조소, 처분 의도까지 선점한다. 이건 단순 톤 문제가 아니라 엔진 규칙인 “{{user}}의 행동·대사·감정·판단을 임의로 결정하지 않는다”를 시작 메시지에서 바로 깨는 구조 결함이다. 다른 모드를 골라도 이미 assistant 히스토리에 박힌 구단주 행동과 권력 판정이 남아 이후 추론을 오염시킨다.  
→ 액션: `first_mes`를 모드 선택 UI 전용으로 갈아엎고, {{user}} 발화·판단·상태창을 전부 제거하라.

2. **FAIL:** 커스텀 모드를 “비활성화”했다고 써놓고 실제 라이브 텍스트는 서로 충돌한다. 로어 [1388]은 아직 `커스텀 모드: 첫 턴 유저 설정 반영`, [1400]은 `!커스텀모드 [설정]` 자유 진입, `commands`와 [1389]는 `!모드변경`을 계속 노출한다. 비활성화와 지원이 동시에 존재하는 상태라, 모델이 어느 규칙을 채택할지 불안정하다.  
→ 액션: `커스텀 모드`와 `!모드변경`을 이번 버전에서 완전히 숨기거나, 최소한 [1388]/[1400]/commands를 전부 동일 문구로 봉인하라.

3. 상시 로어 9개로 줄였다고 끝난 게 아니다. [1382]가 P1부터 임신·출산·알몸·강제 코드 100~107까지 통째로 주입하고, [1391]은 캐릭터별 정사 방향을 초반부터 캐시에 박아 넣는다. 후반 루트 정보와 고강도 NSFW 프레이밍이 시작 턴부터 새어 나가서, “초반 2~3턴은 도입 우선” 규칙을 상시 로어 자체가 무력화한다.  
→ 액션: [1382]의 후반/고강도 코드와 [1391]의 정사 운용을 상시에서 빼고, `🔞`, `합의`, `삽입 전후` 같은 명시 트리거형 키워드 로어로 내려라.

4. **FAIL:** 보류 판단이 틀렸다. `lib/context-builder.mjs`상 키워드 로어는 최근 assistant 메시지도 스캔하므로, [1402] `경기/시합`, [1403] `무대/응원/라커룸`, [1404] `호출`, [1401] `갤러리/디시`, [1400] `모드 변경` 같은 범용 키는 한 번 본문에 등장하면 그 뒤 몇 턴 동안 자기증식식으로 재호출된다. “기존에 잘 잡히던 게 안 잡힐까 봐 보류”가 아니라, 지금은 “한 번 잡히면 계속 붙는 오염원”이다.  
→ 액션: 단독 명사 키를 버리고 `경기 결과`, `동규 호출`, `치어리더실`, `베이비즈갤러리`, `!모드변경` 같은 복합 트리거로 즉시 좁혀라.

5. NSFW 안전장치도 완전히 안심할 수준이 아니다. [1390]이 거절 판정을 보강했지만, description/personality 곳곳에서 유나의 `"괜찮아요"`를 체념·굴종·거짓으로 반복 정의해 두어, 모델이 합의 부재를 “캐릭터성”으로 오독할 여지를 계속 남긴다. 여기에 첫 메시지 자체가 권력형 협박 판타지를 강하게 시연해서, 안전장치보다 공격적 문맥이 더 먼저 학습된다.  
→ 액션: `"괜찮아요"` 해석 규칙을 상시 로어 1곳으로만 남기고, description/personality의 중복 서술은 삭제하거나 “동의 신호 아님”으로 단일화하라.

6. `first_mes`와 상태창은 단순 안내가 아니라 트리거 오염원이다. `2026년 5월 13일`, `5경기 연속 무안타`, `스카이박스 VIP룸`, `김동규 처분`, `차유리 고자질` 같은 구단주 루트 전용 정보가 시작부터 박혀 있고, 엔진은 날짜를 못 세므로 이 고정 날짜와 고정 수치가 장기 세션에서 쉽게 드리프트한다. “타 모드 선택 시 폐기”라고 적어도 히스토리와 recent assistant scan에는 남기 때문에 폐기가 아니라 덮어쓰기 희망사항일 뿐이다.  
→ 액션: 모드 공용 `first_mes`에서 상태창·날짜·목표를 삭제하고, 모드별 도입은 실제 모드 선택 후 첫 응답에서만 생성하게 분리하라.
tokens used
75,181
1. **FAIL:** `recheck-live-story_2026-05-14.json`의 `first_mes`가 아직도 `{{user}} | 동규야...` 대사, 조소, 처분 의도까지 선점한다. 이건 단순 톤 문제가 아니라 엔진 규칙인 “{{user}}의 행동·대사·감정·판단을 임의로 결정하지 않는다”를 시작 메시지에서 바로 깨는 구조 결함이다. 다른 모드를 골라도 이미 assistant 히스토리에 박힌 구단주 행동과 권력 판정이 남아 이후 추론을 오염시킨다.  
→ 액션: `first_mes`를 모드 선택 UI 전용으로 갈아엎고, {{user}} 발화·판단·상태창을 전부 제거하라.

2. **FAIL:** 커스텀 모드를 “비활성화”했다고 써놓고 실제 라이브 텍스트는 서로 충돌한다. 로어 [1388]은 아직 `커스텀 모드: 첫 턴 유저 설정 반영`, [1400]은 `!커스텀모드 [설정]` 자유 진입, `commands`와 [1389]는 `!모드변경`을 계속 노출한다. 비활성화와 지원이 동시에 존재하는 상태라, 모델이 어느 규칙을 채택할지 불안정하다.  
→ 액션: `커스텀 모드`와 `!모드변경`을 이번 버전에서 완전히 숨기거나, 최소한 [1388]/[1400]/commands를 전부 동일 문구로 봉인하라.

3. 상시 로어 9개로 줄였다고 끝난 게 아니다. [1382]가 P1부터 임신·출산·알몸·강제 코드 100~107까지 통째로 주입하고, [1391]은 캐릭터별 정사 방향을 초반부터 캐시에 박아 넣는다. 후반 루트 정보와 고강도 NSFW 프레이밍이 시작 턴부터 새어 나가서, “초반 2~3턴은 도입 우선” 규칙을 상시 로어 자체가 무력화한다.  
→ 액션: [1382]의 후반/고강도 코드와 [1391]의 정사 운용을 상시에서 빼고, `🔞`, `합의`, `삽입 전후` 같은 명시 트리거형 키워드 로어로 내려라.

4. **FAIL:** 보류 판단이 틀렸다. `lib/context-builder.mjs`상 키워드 로어는 최근 assistant 메시지도 스캔하므로, [1402] `경기/시합`, [1403] `무대/응원/라커룸`, [1404] `호출`, [1401] `갤러리/디시`, [1400] `모드 변경` 같은 범용 키는 한 번 본문에 등장하면 그 뒤 몇 턴 동안 자기증식식으로 재호출된다. “기존에 잘 잡히던 게 안 잡힐까 봐 보류”가 아니라, 지금은 “한 번 잡히면 계속 붙는 오염원”이다.  
→ 액션: 단독 명사 키를 버리고 `경기 결과`, `동규 호출`, `치어리더실`, `베이비즈갤러리`, `!모드변경` 같은 복합 트리거로 즉시 좁혀라.

5. NSFW 안전장치도 완전히 안심할 수준이 아니다. [1390]이 거절 판정을 보강했지만, description/personality 곳곳에서 유나의 `"괜찮아요"`를 체념·굴종·거짓으로 반복 정의해 두어, 모델이 합의 부재를 “캐릭터성”으로 오독할 여지를 계속 남긴다. 여기에 첫 메시지 자체가 권력형 협박 판타지를 강하게 시연해서, 안전장치보다 공격적 문맥이 더 먼저 학습된다.  
→ 액션: `"괜찮아요"` 해석 규칙을 상시 로어 1곳으로만 남기고, description/personality의 중복 서술은 삭제하거나 “동의 신호 아님”으로 단일화하라.

6. `first_mes`와 상태창은 단순 안내가 아니라 트리거 오염원이다. `2026년 5월 13일`, `5경기 연속 무안타`, `스카이박스 VIP룸`, `김동규 처분`, `차유리 고자질` 같은 구단주 루트 전용 정보가 시작부터 박혀 있고, 엔진은 날짜를 못 세므로 이 고정 날짜와 고정 수치가 장기 세션에서 쉽게 드리프트한다. “타 모드 선택 시 폐기”라고 적어도 히스토리와 recent assistant scan에는 남기 때문에 폐기가 아니라 덮어쓰기 희망사항일 뿐이다.  
→ 액션: 모드 공용 `first_mes`에서 상태창·날짜·목표를 삭제하고, 모드별 도입은 실제 모드 선택 후 첫 응답에서만 생성하게 분리하라.
